<%- include('../layouts/header') %>

<style>
    body {
        background-color: #f5f5f5;
        display: flex;
        align-items: center;
        justify-content: center;
        min-height: 100vh;
        margin: 0;
    }

    .container {
        max-width: 400px;
        padding: 40px;
        background-color: #ffffff;
        border-radius: 8px;
        box-shadow: 0px 4px 6px rgba(0, 0, 0, 0.1);
    }

    .form-control {
        margin-bottom: 20px;
        padding: 10px;
        border: 1px solid #ccc;
        border-radius: 4px;
        width: 100%;
        font-size: 16px;
    }

    .btn-primary {
        background-color: #4b7bec;
        border: none;
        width: 100%;
        padding: 10px;
        border-radius: 4px;
        color: #ffffff;
        font-size: 16px;
        cursor: pointer;
    }

    .btn-primary:hover {
        background-color: #386cea;
    }

    .alert {
        background-color: #f7dc6f;
        color: #333333;
        border: none;
        padding: 10px;
        border-radius: 4px;
        font-size: 14px;
        text-align: center;
    }
</style>

<div class="container">
    <div class="text-center">
        <!-- <h3 class="pb-3 fw-bold text-danger">snap<span class="text-success">Site</span></h3> -->
    </div>
    <form action="/login" method="post">
        <h4 class="mb-4">Log In</h4>
        <% if(locals.message){ %>
            <div class="alert alert-warning">
                <%= locals.message %>
            </div>
        <% } %>
        <input type="email" name="email" class="form-control" placeholder="Email address" required>
        <input type="password" name="password" class="form-control" placeholder="Password" required>
        <button type="submit" class="btn btn-primary">Login</button>
        <p class="text-center mt-3">Don't have an account? <a href="/register">Signup</a></p>
    </form>
</div>

<%- include('../layouts/footer') %>
